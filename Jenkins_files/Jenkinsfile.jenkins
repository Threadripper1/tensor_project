pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage('Initialize'){
            steps{
                def dockerHome = tool 'myDocker'
                env.PATH = "${dockerHome}/bin:${env.PATH}"
            }
        }
        stage('Test') {
            steps {
                sh 'echo "I love Ukrainewe"'
                sh 'pwd'
                sh 'pytest -q TrainerModel.py'
            }
        }
    }
}